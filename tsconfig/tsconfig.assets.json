{
	"$schema": "https://json.schemastore.org/tsconfig",
	"compilerOptions": {
		"emitDeclarationOnly": false,
		"rootDir": "${configDir}/src",
		"outDir": "${configDir}/.tsc/dist",
		"tsBuildInfoFile": "${configDir}/.tsc/tsconfig.assets.tsbuildinfo"
	},
	"include": ["${configDir}/src/**/*"]
}
