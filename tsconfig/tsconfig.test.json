{
	"$schema": "https://json.schemastore.org/tsconfig",
	"compilerOptions": {
		"rootDir": "${configDir}/src",
		"outDir": "${configDir}/.tsc/dist",
		"tsBuildInfoFile": "${configDir}/.tsc/tsconfig.test.tsbuildinfo"
	},
	"include": ["${configDir}/src/**/*.test.*"]
}
