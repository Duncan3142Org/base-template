name: "Install NPM Release CLI"
description: "Installs the NPM Release CLI"
runs:
  using: "composite"
  steps:
    - name: Install Package
      shell: bash
      working-directory: ${{ github.action_path }}
      run: npm i -g
