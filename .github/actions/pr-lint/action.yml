name: "Install PR Lint CLI"
description: "Installs the PR Lint CLI"
runs:
  using: "composite"
  steps:
    - name: Install Package
      shell: bash
      working-directory: ${{ github.action_path }}
      run: |
        npm ci
        npm i -g
