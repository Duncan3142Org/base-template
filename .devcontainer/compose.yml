include:
  - ./compose.otel.yml

x-config:
  terraform_plugin_cache_path: &terraform_plugin_cache_path /root/.terraform.d/plugin-cache

volumes:
  node_modules:
  terraform_env_workspace:
  local_bin:
  mise_state:
  copilot_state:
  mise_data:
    name: shared-mise-data
  mise_cache:
    name: shared-mise-cache
  apt_cache:
    name: shared-apt-cache
  npm_cache:
    name: shared-npm-cache
  terraform_plugin_cache:
    name: shared-terraform-plugin-cache

services:
  devcon:
    image: mcr.microsoft.com/devcontainers/base:ubuntu-24.04

    # Keep container running indefinitely
    command: sleep infinity

    environment:
      TF_PLUGIN_CACHE_DIR: *terraform_plugin_cache_path
      MISE_ENV: dev
      DBUS_SESSION_BUS_ADDRESS: unix:path=/run/dbus-proxy/secrets.sock
      GITHUB_USERNAME: ${GITHUB_USERNAME:?}

    # Mount project folder and Docker socket
    volumes:
      - ..:/workspace
      - ./devcon/.npmrc:/root/.npmrc
      - ./devcon/profile.d/mise-tokens.sh:/etc/profile.d/mise-tokens.sh
      - ./devcon/apt/sources.list.d/docker.sources:/etc/apt/sources.list.d/docker.sources
      - ./runtime:/run/dbus-proxy
      - ${XDG_RUNTIME_DIR}/docker.sock:/var/run/docker.sock
      - ${HOME}/.terraform.d/credentials.tfrc.json:/root/.terraform.d/credentials.tfrc.json

      - mise_state:/root/.local/state/mise
      - copilot_state:/root/.copilot
      - node_modules:/workspace/node_modules
      - terraform_env_workspace:/workspace/.github/environments/.terraform
      - local_bin:/root/.local/bin

      - mise_data:/root/.local/share/mise
      - mise_cache:/root/.cache/mise
      - apt_cache:/var/cache/apt
      - npm_cache:/root/.npm
      - type: volume
        source: terraform_plugin_cache
        target: *terraform_plugin_cache_path

    env_file:
      - ./devcon/.env
    networks:
      - otel
